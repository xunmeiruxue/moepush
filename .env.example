AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
DISABLE_REGISTER=false

# 通用 OAuth2/OIDC 配置（可选，设置后将使用通用 OAuth 代替 GitHub）
# 适用于 Authentik、Keycloak、Auth0 等 OIDC 兼容的身份提供商
# 回调 URL: https://your-domain.com/api/auth/callback/oauth
# AUTH_OAUTH_ID=
# AUTH_OAUTH_SECRET=
# AUTH_OAUTH_ISSUER=https://your-oauth-provider.com/application/o/your-app-slug
# AUTH_OAUTH_NAME=Authentik  # 按钮显示名称，如 "Authentik"、"SSO" 等